<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Omnitrix" parent="Theme.MaterialComponents.NoActionBar">
        <item name="colorPrimary">@color/omnitrix_green</item>
        <item name="colorPrimaryDark">@color/black</item>
        <item name="colorAccent">@color/omnitrix_glow</item>
        <item name="android:windowBackground">@color/screen_bg</item>
    </style>

    <!-- Fullscreen, no status/nav bar clutter — this is a watch, every pixel counts -->
    <style name="Theme.Omnitrix.Fullscreen" parent="Theme.Omnitrix">
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowActionBar">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowLayoutInDisplayCutoutMode" tools:targetApi="27">shortEdges</item>
    </style>
</resources>
